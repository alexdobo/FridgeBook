
//init a array to store vol measurements
public int volumn[] = new Part[10000000];

//skelenton

//measure 2wo times a day(light)
//regular use
//0:00, 8:00, 16:00
//take volumn at 0:00 everyday


for(int i; i<1000000000;i++){
  volumn[i]=reader.nextInt();
}

//quantity
//how much milk left






//boolean 
//returns buy milk or not (light)
//if milk volume is less than 50ml, send the msg






//stats
//calculate average daily milk consumption
//if we were to use a wk's data 
//avg = [sum(v(1:7))]/7 days
//avg 

//===================================================================
//days(full) for detailed notice
//day= current quantity/V milk avg
int daysLast (int currentMilk, int avgConsumption){
  int days = (int) currentMilk/avgConsumption; //or round()?
  return days;
}
//===================================================================
//send a msg every two hours if not enough milk(light)

//warning flag (full)



//====================================================================

//if we were to call the measure funtion on a specific time

#include <Time.h>
#include <TimeAlarms.h>

void Alarm ( ){
  Alarm.alarmRepeat(/*hours8*/, /*minutes*/, /*seconds*/, /*function*/);
}
//====================================================================
